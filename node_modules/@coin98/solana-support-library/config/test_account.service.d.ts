import { Keypair } from '@solana/web3.js';
export declare class TestAccountService {
    static getAccount(num: number): Promise<Keypair>;
    static getProgramAccount(num: number): Promise<Keypair>;
    static getTokenAccount(num: number): Promise<Keypair>;
    static getNamedTokenAccount(tokenName: TokenName): Keypair;
}
export declare enum TokenName {
    ATOM = 0,
    BNB = 1,
    BTC = 2,
    BUSD = 3,
    C98 = 4,
    CUSD = 5,
    DOT = 6,
    ETH = 7,
    FTT = 8,
    SOL = 9,
    SRM = 10,
    USDC = 11,
    USDT = 12
}
