import { Connection, Keypair, NonceAccount, PublicKey, Transaction } from "@solana/web3.js";
export declare class DurableNonceService {
    static createNonceAccount(connection: Connection, nonceAccount: Keypair, payerAccount: Keypair, authorityAddress: PublicKey): Promise<string | any>;
    static getNonceAccount(connection: Connection, nonceAddress: PublicKey): Promise<NonceAccount>;
    static createNonceTransaction(connection: Connection, nonceAddress: PublicKey, authorityAddress: PublicKey): Promise<Transaction>;
}
