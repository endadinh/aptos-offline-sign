import { Connection, Keypair, PublicKey } from '@solana/web3.js';
import { TransactionLog } from './interfaces';
export declare class SolanaService {
    static getAccountBalance(connection: Connection, address: PublicKey): Promise<void>;
    static getMinimumBalanceForRentExemption(connection: Connection, space: number): Promise<number>;
    static getTransactionLogMessages(connection: Connection, txSignature: string): Promise<TransactionLog>;
    static formatLogMessages(messages: string[]): TransactionLog;
    static generateKeypairFromSeed(fromPublicKey: PublicKey, seed: string, programId: PublicKey): Promise<Keypair>;
    static isAddressAvailable(connection: Connection, address: PublicKey): Promise<boolean>;
    static isAddressInUse(connection: Connection, address: PublicKey): Promise<boolean>;
    static isProgramAccount(connection: Connection, address: PublicKey): Promise<boolean>;
}
