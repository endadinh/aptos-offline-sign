"use strict";
var __read = (this && this.__read) || function (o, n) {
    var m = typeof Symbol === "function" && o[Symbol.iterator];
    if (!m) return o;
    var i = m.call(o), r, ar = [], e;
    try {
        while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
    }
    catch (error) { e = { error: error }; }
    finally {
        try {
            if (r && !r.done && (m = i["return"])) m.call(i);
        }
        finally { if (e) throw e.error; }
    }
    return ar;
};
var __spreadArray = (this && this.__spreadArray) || function (to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
        if (ar || !(i in from)) {
            if (!ar) ar = Array.prototype.slice.call(from, 0, i);
            ar[i] = from[i];
        }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.BufferLayoutService = void 0;
var buffer_1 = require("buffer");
var hash_service_1 = require("./hash.service");
var BufferLayoutService = /** @class */ (function () {
    function BufferLayoutService() {
    }
    BufferLayoutService.encodeAnchorInstruction = function (method, instruction, layout) {
        var prefix = hash_service_1.HashService.sha256("global:".concat(method));
        var truncatedPrefix = prefix.slice(0, 8);
        var buffer = buffer_1.Buffer.alloc(layout.span);
        var span = layout.encode(instruction, buffer);
        return buffer_1.Buffer.from(__spreadArray(__spreadArray([], __read(truncatedPrefix), false), __read(buffer.slice(0, span)), false));
    };
    BufferLayoutService.encodeInstruction = function (instruction, layout) {
        var buffer = buffer_1.Buffer.alloc(layout.span);
        var span = layout.encode(instruction, buffer);
        return buffer.slice(0, span);
    };
    BufferLayoutService.encodeUnionInstruction = function (instruction, layout) {
        var instructionMaxSpan = Math.max.apply(Math, __spreadArray([], __read(Object.values(layout.registry).map(function (r) { return r.span; })), false));
        var buffer = buffer_1.Buffer.alloc(instructionMaxSpan);
        var span = layout.encode(instruction, buffer);
        return buffer.slice(0, span);
    };
    return BufferLayoutService;
}());
exports.BufferLayoutService = BufferLayoutService;
