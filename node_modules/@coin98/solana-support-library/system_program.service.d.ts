import { Connection, Keypair, PublicKey } from '@solana/web3.js';
export declare class SystemProgramService {
    static transfer(connection: Connection, payerAccount: Keypair, recipientAddress: PublicKey, amount: number): Promise<boolean>;
}
